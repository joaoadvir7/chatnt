# Diário

Menu do usuário → Encerrar o dia / diário. PIN SHA-256 `chatnt-pin:{pin}`.

## 22/08/2026

- Ice breakers oficiais; loop #185 corrigido.
- Coexistência documentada (API cobrada; app não).
- Menu da conta: mapa, docs, políticas, diário + PIN.
- GitHub `main` ainda é Next+Prisma de 10/08 — **não** contém o código deste preview.

Pendências: backup do `src/` Grok no GitHub; unificar stacks; webhook persistente; tokens no servidor.
