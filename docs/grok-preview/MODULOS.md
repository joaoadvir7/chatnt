# Módulos (tela → arquivo)

Ver tabela em HANDOFF. Rotas: `/painel` `/funil` `/contatos` `/live-chat` `/automacoes` `/automacoes/$id` `/broadcasts` `/ia` `/crm` `/conexoes` `/atendentes` `/webhooks` `/monitoramento` `/meta` `/sedes`.

Canvas: um único `FlowWorkspace` (prop `embedded` no broadcast). Portas no topo. Reply ramifica; link não. Encaminhar não lista `source === "broadcast"`.
